#include <stdio.h>

typedef char com;
int main(){
com a;
com b = 'a';
com c = 'b';
printf("%c %c %c\n", a, b, c);
}

// typedef is used to create an variable for a data type.
// Here I created a variable name com of char data type.
// I can use com instead of char for declaring variables.
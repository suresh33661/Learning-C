#include <stdio.h>

int main(){
    int price[] = {7,6,4,3,1};
    int len = sizeof(price)/sizeof(price[0]);

   int buy = 0;
   int sell = 1;i
   int profit = 0;
   while (sell < len-1){
    if(price[buy])
   }
}